declare const STATE_HANDLERS: {
    contain: (timeout: number, type: 'url' | 'path', reverse: boolean, expectedValue: string) => true | void;
    match: (timeout: number, type: 'url' | 'path', reverse: boolean, expectedValue: string) => true | void;
};
declare type State = keyof typeof STATE_HANDLERS;
declare const _default: (timeout: number | undefined, type: 'url' | 'path', reverse: boolean, state: State, expectedValue: string) => void;
/**
 * Wait for the url or path to contain or match the specified value
 * @param [timeout=3000]    Wait duration (optional)
 * @param type              The type to check (url or path)
 * @param reverse           Check for opposite state
 * @param state             State to check for (contain or match)
 * @param expectedValue     The value to check for
 *
 * @todo make fallback timeout configurable
 * @todo add some tests that use this
 */
export default _default;
